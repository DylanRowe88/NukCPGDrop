<div class="logo-display">
    <img src="/images/nuks-logo.png"
         alt="Nuks Logo"
         class="logo-img"
         onerror="this.style.display='none'" />
    <div class="logo-fallback">
        <span class="logo-text">NUKCPGDROP</span>
    </div>
</div>

<style>
    .logo-display {
        display: flex;
        align-items: center;
        justify-content: center;
        padding: 1rem;
    }
    .logo-img {
        max-width: clamp(120px, 30vw, 300px);
        height: auto;
        object-fit: contain;
    }
    .logo-text {
        font-size: clamp(1.2rem, 4vw, 2.5rem);
        font-weight: 800;
        letter-spacing: 0.15em;
        color: var(--accent-color, #e63946);
    }
</style>
