<div class="logo-display">
    <a href="https://canonuks.com" target="_blank" rel="noopener noreferrer"
       class="logo-link" title="Nuks Nootropics">
        <img src="/images/Nuks_Logo_Final_White.png"
             alt="Nuks Logo"
             class="logo-img"
             onerror="this.src='/images/nuks-logo.png'" />
    </a>
    <div class="logo-fallback">
        <span class="logo-text">NUKCPGDROP</span>
    </div>
    <div class="rest-bar">
        <div class="rest-content">
            <img src="/images/nuks-possum-white.png" alt="" class="rat-img" aria-hidden="true" />
            <span class="rest-text">REST WHEN YOU'RE DEAD</span>
            <img src="/images/nuks-possum-white.png" alt="" class="rat-img flipped" aria-hidden="true" />
            <img src="/images/nuks-possum-white.png" alt="" class="rat-img" aria-hidden="true" />
            <span class="rest-text">REST WHEN YOU'RE DEAD</span>
            <img src="/images/nuks-possum-white.png" alt="" class="rat-img flipped" aria-hidden="true" />
        </div>
    </div>
</div>

<style>
    .logo-display {
        display: flex;
        flex-direction: column;
        align-items: center;
        justify-content: center;
        padding: 0.75rem 1rem 0.5rem;
        gap: 0.15rem;
    }
    .logo-link {
        display: flex;
        justify-content: center;
        cursor: pointer;
        transition: opacity 0.2s;
        text-decoration: none;
    }
    .logo-link:hover { opacity: 0.8; }
    .logo-img {
        max-width: clamp(100px, 25vw, 220px);
        height: auto;
        object-fit: contain;
    }
    .logo-fallback {
        display: flex;
        justify-content: center;
        width: 100%;
    }
    .logo-text {
        font-size: clamp(0.9rem, 2.5vw, 1.5rem);
        font-weight: 800;
        letter-spacing: 0.15em;
        color: var(--accent-color, #e63946);
        text-align: center;
    }
    .rest-bar {
        display: flex;
        align-items: center;
        gap: 2rem;
        padding: 0.5rem 0;
        width: 100%;
        overflow: hidden;
        white-space: nowrap;
        mask-image: linear-gradient(90deg, transparent, #000 5%, #000 95%, transparent);
        -webkit-mask-image: linear-gradient(90deg, transparent, #000 5%, #000 95%, transparent);
    }
    .rest-content {
        display: flex;
        align-items: center;
        gap: 2rem;
        animation: marquee-scroll 12s linear infinite;
    }
    .rat-img {
        height: clamp(22px, 4vw, 36px);
        width: auto;
        opacity: 0.6;
    }
    .rat-img.flipped { transform: scaleX(-1); }
    .rest-text {
        font-size: clamp(0.7rem, 2vw, 0.9rem);
        font-weight: 700;
        letter-spacing: 0.25em;
        color: var(--text-muted, #777);
        text-transform: uppercase;
    }
    @@keyframes marquee-scroll {
        0% { transform: translateX(0); }
        100% { transform: translateX(-50%); }
    }
</style>
