<div class="logo-display">
    <a href="https://canonuks.com" target="_blank" rel="noopener noreferrer"
       class="logo-link" title="Nuks Nootropics">
        <img src="/images/Nuks_Logo_Final_White.png"
             alt="Nuks Logo"
             class="logo-img"
             onerror="this.src='/images/nuks-logo.png'" />
    </a>
    <div class="logo-fallback">
        <span class="logo-text">NUKCPGDROP</span>
    </div>
    <div class="rest-bar">
        <img src="/images/nuks-possum-white.png" alt="" class="rat-img" aria-hidden="true" />
        <span class="rest-text">REST WHEN YOU'RE DEAD</span>
        <img src="/images/nuks-possum-white.png" alt="" class="rat-img flipped" aria-hidden="true" />
    </div>
</div>

<style>
    .logo-display {
        display: flex;
        flex-direction: column;
        align-items: center;
        justify-content: center;
        padding: 0.75rem 1rem 0.5rem;
        gap: 0.15rem;
    }
    .logo-link {
        display: flex;
        justify-content: center;
        cursor: pointer;
        transition: opacity 0.2s;
        text-decoration: none;
    }
    .logo-link:hover { opacity: 0.8; }
    .logo-img {
        max-width: clamp(100px, 25vw, 220px);
        height: auto;
        object-fit: contain;
    }
    .logo-fallback {
        display: flex;
        justify-content: center;
        width: 100%;
    }
    .logo-text {
        font-size: clamp(0.9rem, 2.5vw, 1.5rem);
        font-weight: 800;
        letter-spacing: 0.15em;
        color: var(--accent-color, #e63946);
        text-align: center;
    }
    .rest-bar {
        display: flex;
        align-items: center;
        justify-content: center;
        gap: 0.75rem;
        padding: 0.5rem 0;
        width: 100%;
        overflow: hidden;
        white-space: nowrap;
    }
    .rat-img {
        height: clamp(22px, 4vw, 36px);
        width: auto;
        opacity: 0.6;
        animation: rat-scroll 8s ease-in-out infinite;
    }
    .rat-img.flipped {
        transform: scaleX(-1);
        animation: rat-scroll-flipped 8s ease-in-out infinite;
    }
    .rest-text {
        font-size: clamp(0.7rem, 2vw, 0.9rem);
        font-weight: 700;
        letter-spacing: 0.25em;
        color: var(--text-muted, #777);
        text-transform: uppercase;
        animation: text-pulse 4s ease-in-out infinite;
    }
    @@keyframes rat-scroll {
        0%, 100% { transform: translateX(-5px); opacity: 0.6; }
        50% { transform: translateX(5px); opacity: 1; }
    }
    @@keyframes rat-scroll-flipped {
        0%, 100% { transform: scaleX(-1) translateX(5px); opacity: 0.6; }
        50% { transform: scaleX(-1) translateX(-5px); opacity: 1; }
    }
    @@keyframes text-pulse {
        0%, 100% { opacity: 0.7; letter-spacing: 0.25em; }
        50% { opacity: 1; letter-spacing: 0.3em; }
    }
</style>
